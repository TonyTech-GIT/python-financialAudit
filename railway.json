{
    "build": {
      "builder": "DOCKERFILE",
      "dockerfilePath": "Dockerfile",
      "staticPath": "staticfiles",
    "staticUrl": "/static"
    },

    "healthcheck": {
    "path": "/health/",
    "method": "GET",
    "interval": 30,
    "timeout": 10,
    "initialDelay": 30
  }
  }